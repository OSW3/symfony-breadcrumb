<ol {{_get_attributes(_attr)}}>
{% for item in items %}
    <twig:BreadcrumbItem 
        :label="item.label" 
        :icon="item.icon" 
        :active="loop.last" 
        :route="item.route" 
    />
{% endfor %}
</ol>