{% extends "@Breadcrumb/Breadcrumb.html.twig" %}

{% set _attr = attr|default({}) %}
{% set _attr = _add_attribute(_attr, 'id', id) %}
{% set _attr = _add_attribute(_attr, 'class', class) %}
{% set _attr = _add_attribute(_attr, 'aria-label', "breadcrumb") %}
{% set _disabled = disabled %}
